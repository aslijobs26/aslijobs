/**
 * Settings helpers for codebase-backed surfaces only.
 * Preference/plan stubs were removed — those APIs do not exist yet.
 */
export const employerSettingsQueryKeys = {
  all: ["employer", "settings"] as const,
};
