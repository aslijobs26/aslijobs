export type NavItem = {
  label: string;
  href: string;
  hasChevron: boolean;
};

/** Public header center nav — intentionally empty for the current landing header. */
export const NAV_ITEMS: NavItem[] = [];
