import { Router } from "express";

/**
 * Dashboard sub-routes for the operations module.
 * Mount under operationsRouter when implemented.
 */
export const operationsDashboardRouter = Router();

// Future:
// operationsDashboardRouter.get("/overview", asyncHandler(operationsDashboardController.overview));

export default operationsDashboardRouter;
